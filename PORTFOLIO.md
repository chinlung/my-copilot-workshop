# 待辦清單 Web App

這是一個在 GitHub Copilot 實戰工作坊中完成的待辦清單 Web App。它以簡潔的介面協助使用者新增、整理與追蹤日常待辦事項，並將資料保存在使用者自己的瀏覽器中。

## 線上展示

https://<你的帳號>.github.io/<你的repo名稱>/

> 請將上方網址替換成實際的 GitHub Pages 網址。

## 功能

- 新增待辦事項，空白內容不會建立項目。
- 勾選待辦事項以標記完成，也可以取消完成狀態。
- 以刪除線和淡化樣式區分已完成項目。
- 刪除單筆待辦事項。
- 一次清除所有已完成的待辦事項，執行前會要求確認。
- 依照「全部」、「未完成」或「已完成」篩選清單。
- 即時顯示未完成項目的數量。
- 根據目前篩選條件顯示對應的空清單提示。
- 支援深色模式與淺色模式切換。
- 尚未手動選擇主題時，會依照作業系統的外觀偏好顯示。
- 記住使用者選擇的主題與待辦資料，重新整理頁面後仍可保留。
- 支援手機螢幕與鍵盤操作。

## 技術

- 使用純 HTML、CSS 與原生 JavaScript 建立。
- 不使用任何前端框架、第三方套件或外部 CDN。
- 不需要建置工具，可直接在瀏覽器中離線開啟。
- 使用瀏覽器 `localStorage` 儲存待辦事項與主題偏好。
- 使用 CSS 變數與 `prefers-color-scheme` 支援深色與淺色主題。
- 主要檔案分工如下：
  - `index.html`：頁面結構與互動元素。
  - `styles.css`：版面、響應式樣式與主題外觀。
  - `app.js`：待辦清單操作、篩選、主題切換與資料儲存。

## 開發方式

這個專案依照工作坊的 agentic workflow 完成，將 AI 的協作能力、外部工具連線與可重複的工作流程結合起來：

- **GitHub Copilot Agent Mode**：從待辦清單的需求開始，協助建立和修改 `index.html`、`styles.css` 與 `app.js`，也能根據執行結果持續調整多個檔案。
- **MCP**：透過 `.vscode/mcp.json` 設定 Microsoft Learn 與 GitHub MCP Server，讓 Copilot 能查詢官方文件，以及讀取 GitHub issue 等專案脈絡。
- **`.github/copilot-instructions.md`**：定義專案的技術限制、程式風格與協作規則，讓每次協作都遵循同一套約定。
- **`.github/prompts/fix-issue.prompt.md`**：把讀取 issue、提出計畫、修改程式、驗證與建立 Pull Request 的步驟整理成可重複使用的任務劇本。

## 我學到什麼

- Agent Mode 的重點是交代目標與限制，讓 AI 協助規劃並執行跨檔案的工作。
- 清楚的專案指引能讓程式風格、技術限制與協作流程保持一致。
- MCP 能把文件與 GitHub 專案資訊接到 Copilot 的工作流程中，提供更完整的上下文。
- 將重複性的多步驟工作寫成 prompt，能讓流程更容易重複、檢查與分享。
- 使用版本控制與驗證步驟，可以在採用 AI 修改前後保留清楚的檢查點。

## 授權

MIT